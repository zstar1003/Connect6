# ==========================================
# Local Development Configuration
# ==========================================

# PeerJS Server Configuration for LAN Play
# Set to 'true' to enable local network multiplayer, 'false' to use public cloud server
VITE_USE_LAN_SERVER=true

# PeerJS server settings (only used when VITE_USE_LAN_SERVER=true)
# For same machine testing: use 'localhost'
# For LAN multiplayer: use your computer's local IP address
#   - macOS/Linux: Find IP with: ifconfig | grep "inet "
#   - Windows: Find IP with: ipconfig
#   - Example: 192.168.1.100
VITE_PEER_HOST=localhost
VITE_PEER_PORT=9000
VITE_PEER_PATH=/myapp

# Room Server Port (HTTP API for room discovery)
VITE_ROOM_SERVER_PORT=9001

# ==========================================
# Production Configuration (Cloudflare)
# ==========================================
# For production deployment, create a .env.production file with:
#
# VITE_USE_LAN_SERVER=true
# VITE_PEER_HOST=0.peerjs.com
# VITE_PEER_PORT=443
# VITE_PEER_PATH=/
# VITE_ROOM_API_URL=https://connect6-room-api.YOUR_SUBDOMAIN.workers.dev
#
# Replace YOUR_SUBDOMAIN with your actual Cloudflare Workers subdomain
